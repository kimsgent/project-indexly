default_language_version:
  python: python3

repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run pytest before commit
        entry: .venv-indexly/Scripts/python.exe -m pytest -v
        language: system
        types: [python]
        pass_filenames: false
