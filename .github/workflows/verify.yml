# .github/workflows/verify.yml
name: Verify (Preflight)

on:
  push:
    branches: [ "**" ]
  pull_request:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build sdist + wheel
        run: python -m build

      - name: Twine check
        run: twine check dist/*

      - name: Generate brew formula (dry-run)
        env:
          VERSION: v0.0.0-test
          DRY_RUN: "1"
        run: |
          python scripts/generate_brew_formula.py \
            --source dist/*.tar.gz \
            --dry-run \
            --out Formula/indexly.rb

  homebrew-audit:
    name: Homebrew Formula Audit
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check formula syntax
        run: ruby -c Formula/indexly.rb

      - name: Audit formula in an isolated tap
        run: |
          brew update
          brew tap-new --no-git indexly/ci
          cp Formula/indexly.rb "$(brew --repository indexly/ci)/Formula/indexly.rb"
          brew audit --strict indexly/ci/indexly
