---
title: "Release v2.1.5"
type: docs
toc: true
weight: 10
date: "2026-07-11"
release_id: IDXREL-2.1.5
version: "2.1.5"
released_at: "2026-07-11"
area_ids: ["IDX-01", "IDX-02", "IDX-03", "IDX-04", "IDX-08", "IDX-12"]
risk_ids: ["IDX-RISK-001", "IDX-RISK-002", "IDX-RISK-006", "IDX-RISK-007", "IDX-RISK-009", "IDX-RISK-012"]
mapping_confidence: high
source_commit_range: "v2.1.4..2b56c1a1a0f487b89ae3cb535caaaafb8b0b6ed7"
description: "Indexly v2.1.5 adds safe incremental indexing, log-based scope, read-only plan previews, quieter Excel processing, and reviewed release metadata checks."
keywords:
  - Indexly v2.1.5
  - incremental indexing
  - index plan
  - index logs
  - Excel warnings
  - release metadata
  - release notes
tags:
  - release
  - indexing
  - logging
  - excel
  - documentation
change_ids:
  - id: IDXREL-2.1.5-CHG-001
    type: fix
    area_ids: ["IDX-02", "IDX-04"]
    risk_ids: ["IDX-RISK-006"]
    summary: "Scoped suppression to known benign openpyxl feature warnings during Excel extraction and analysis while preserving unrelated warnings."
  - id: IDXREL-2.1.5-CHG-002
    type: feature
    area_ids: ["IDX-01", "IDX-02", "IDX-03"]
    risk_ids: ["IDX-RISK-001", "IDX-RISK-002"]
    summary: "Added incremental indexing that uses stored filesystem stat fingerprints to skip unchanged files while retaining safe stale-row pruning."
  - id: IDXREL-2.1.5-CHG-003
    type: feature
    area_ids: ["IDX-01", "IDX-02", "IDX-08"]
    risk_ids: ["IDX-RISK-001", "IDX-RISK-002", "IDX-RISK-009"]
    summary: "Added month and custom-log scoping for incremental indexing and preserved exact Windows and POSIX paths in index events."
  - id: IDXREL-2.1.5-CHG-004
    type: feature
    area_ids: ["IDX-01", "IDX-02", "IDX-03"]
    risk_ids: ["IDX-RISK-001", "IDX-RISK-002", "IDX-RISK-007"]
    summary: "Added a read-only index plan that reports scan, scope, skip, index, and stale-row counts without changing the database or logs."
  - id: IDXREL-2.1.5-CHG-005
    type: docs
    area_ids: ["IDX-01", "IDX-02", "IDX-08", "IDX-12"]
    risk_ids: ["IDX-RISK-001", "IDX-RISK-002", "IDX-RISK-009", "IDX-RISK-012"]
    summary: "Documented incremental indexing workflows and enriched reviewed metadata for v2.1.0 through v2.1.4 and selected archived release pages."
  - id: IDXREL-2.1.5-CHG-006
    type: tooling
    area_ids: ["IDX-12"]
    risk_ids: ["IDX-RISK-012"]
    summary: "Added advisory checks for release-page metadata and the latest documentation highlights without making publication depend on the validator."
---

## Release v2.1.5 (2026-07-11)

Indexly v2.1.5 adds faster repeat indexing for stable folders, scopes re-indexing from prior index logs, and provides a non-mutating preview of planned index work. It also reduces benign Excel warning noise and strengthens review of release documentation metadata.

### Highlights

- Skips unchanged indexed files with `-r` or `--only-changes` by comparing filesystem stat fingerprints with stored index metadata.
- Scopes indexing to prior `FILE_INDEXED` events with `--month` or `--log-file`, including exact Windows and POSIX path handling.
- Previews scan, scope, skip, index, and stale-row counts with `--plan` without changing the index or writing logs.
- Suppresses known benign openpyxl workbook-feature warnings while leaving unrelated warnings visible.

### Incremental Indexing

- New and edited files continue through the normal indexing path. Legacy index rows without fingerprints retain modified-time compatibility, and files that cannot be checked safely are processed instead of skipped.
- Deleted or newly ignored files remain eligible for stale-row pruning during an incremental run.
- Month and custom-log scope can be combined with `-r`; log scope is applied before unchanged-file filtering.
- Index summaries now record the selected mode and scan, scope, skip, indexed, removed, and stat-error counts.

### Excel Processing

- Excel extraction, sheet discovery, and analysis share a narrow warning scope for known non-fatal openpyxl feature messages.
- Other warning categories and unrelated openpyxl warnings remain visible for diagnosis.

### Documentation and Release Tooling

- The usage guide documents full, incremental, log-scoped, and plan-only indexing workflows.
- Release metadata was enriched for v2.1.0 through v2.1.4 and selected archived pages; historical source ranges remain documented only where verified.
- An agent-side advisory validator checks release metadata and keeps the documentation hub limited to the latest four stable releases.

### Dependency, CI, and Packaging Notes

- No relevant authoritative dependency declaration changes are included in this release range.
- No net release-workflow behavior change is included; metadata validation remains advisory.
- The Homebrew change at the start of the source range belongs to the prior `v2.1.4` publishing workflow and is not part of this release's user-facing changes.

### Breaking Changes

None expected.

### Contributors

- N. K. Franklin-Gent
- OpenAI Codex
